#include <stdio.h>
#include <iostream>
#include<ctype.h>

void  main()
{
	char str[100];
	char c;
	int length,i;
	//scanf("%s",str);
	c=getchar();
	//length=strlen(str);
	if (c == ' ');
	printf("kongge");
	/*for (i = 0; i < length; i++)
	{
		if isspace(str[i])

	}*/






	system("pause");

}
